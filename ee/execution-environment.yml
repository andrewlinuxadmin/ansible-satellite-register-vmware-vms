---
version: 3

images:
  base_image:
    name: registry.redhat.io/ansible-automation-platform-25/ee-minimal-rhel9:latest

additional_build_steps:
  append_final:
    - RUN /usr/bin/python3 -m pip install --no-cache-dir 'pyVmomi>=8.0'

options:
  package_manager_path: /usr/bin/microdnf
